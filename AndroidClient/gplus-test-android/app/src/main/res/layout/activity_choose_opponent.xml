<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="women.hackathon.challengeme.ChooseOpponent" >
    <ListView
        android:id="@+id/circles_list"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="0dip"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_toLeftOf="@+id/ImageButton_my_trophy_button"
        android:layout_toStartOf="@+id/ImageButton_my_trophy_button"
        android:layout_below="@+id/linearLayout"
        android:layout_alignRight="@+id/sign_in_status"
        android:layout_alignEnd="@+id/sign_in_status"
        android:layout_above="@+id/ImageButton_challenge_button" />
     <ImageButton
            android:id="@+id/ImageButton_challenge_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="14dp"
            android:background="@drawable/challenge_button"
            android:onClick="onChallengeButtonClick"
            android:src="@drawable/challenge_button" />

</RelativeLayout>
